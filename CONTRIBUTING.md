# 기여 안내

이 프로젝트는 독립적으로 유지보수하는 eGovFrame 관련 커뮤니티 예제이다.
버그 보고에는 Java 버전, 재현 명령, 가상 입력과 예상·실제 결과를 포함한다.
API 키와 실제 업무 문서는 이슈·PR에 넣지 않는다.

코드 변경은 `mvn -B clean verify`로 확인한다. 테스트에는 로컬 고정 응답 HTTP 서버를 사용하며 외부 모델 비용이 발생하지 않아야 한다.
모델 연결은 `ChatModel` 경계에 두고 공급자별 이름이나 실제 서비스 기본값을 공통 설정에 추가하지 않는다.
기여 코드는 저장소의 Apache-2.0 라이선스로 제공한다.

전자정부 표준프레임워크에서의 공식 인증이나 운영 적합성은 이 저장소의 테스트 범위가 아니다.
